# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/phishing-simulator

# The public base URL for tracking links (must be reachable by email recipients)
TRACKING_BASE_URL=http://localhost:3001

# Gmail SMTP credentials (use a dedicated throwaway Gmail + App Password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Server port (default: 3001)
PORT=3001
