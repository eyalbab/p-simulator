# Optional docker-compose overrides
MONGODB_PORT=27017
TRACKING_BASE_URL=http://localhost:3001
VITE_MANAGEMENT_SERVER_URL=http://localhost:3000

# Shared secret for service-to-service auth (must be the same for both servers)
SERVICE_API_KEY=change-me-to-a-random-api-key
